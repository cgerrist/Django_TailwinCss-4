This is a starter project for Django 6 with TailWindCss 4.
The blog app is just a test app where you can test TailWind. You only have to create a venv with the command 'python3 -m venv .venv' and do a command 'pip install Django' and install tailwind 4. Now you are good to go.

Succes
